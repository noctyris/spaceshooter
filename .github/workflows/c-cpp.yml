name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Compile
      run: make compile
    - name: Run
      run: make run
    - name: Clean
      run: make clean
